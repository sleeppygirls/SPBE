name: Laravel CI/CD

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  laravel-tests:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: "8.0"
          extensions: mbstring, bcmath, pdo_mysql
          ini-values: post_max_size=256M, upload_max_filesize=256M
          coverage: xdebug
          tools: composer

      - name: Install dependencies
        run: composer install --prefer-dist --no-progress --no-suggest

      - name: Copy .env file
        run: cp .env.example .env

      - name: Generate application key
        run: php artisan key:generate

      - name: Run tests
        run: php artisan test

  deploy:
    runs-on: ubuntu-latest
    needs: [laravel-tests]
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Install expect
        run: sudo apt-get install -y expect

      - name: Deploy to Server
        env:
          SERVER_USER: wicaksu
          SERVER_IP: 103.132.230.115
          SERVER_PASSWORD: Jack03061997
          DEPLOY_PATH: /home/wicaksu/spbe
        run: |
          expect <<EOF
          spawn ssh wicaksu@103.132.230.115
          expect "password:"
          send "Jack03061997\r"
          expect "$ "
          send "cd /home/wicaksu/spbe && git pull origin main && composer install --prefer-dist --no-progress --no-suggest && php artisan migrate --force && php artisan optimize\r"
          expect "$ "
          send "exit\r"
          expect eof
          EOF
